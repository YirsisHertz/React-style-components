import React from "react";
import { NavHeader } from "../styled-components/Header/NavHeader";

const Header = () => {
  return (
    <NavHeader>
      <h1>Navigate</h1>
    </NavHeader>
  );
};

export default Header;
